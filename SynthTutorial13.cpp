#include "SynthTutorial13.h"

SynthTutorial13::SynthTutorial13(Inputs* inputs)
{
  /*
  ModuleWavetableOsc *wavetable_osc1 = new ModuleWavetableOsc();
  ModuleWavetableOsc *wavetable_osc2 = new ModuleWavetableOsc();
  ModuleAddNote *add_note = new ModuleAddNote();
  ModuleMixer *mixer = new ModuleMixer();

  // User param 1 and param 2 to select wavetables
  wavetable_osc1->wavetable_input  = inputs->param1;
  wavetable_osc2->wavetable_input  = inputs->param2;

  // The inputs->sr will control both wavetable's frequency
  wavetable_osc1->frequency_input  = inputs->sr;
  
  // Wavetable Oscillator's frequency will be increased by an octave
  add_note->note_input = new ModuleConstant(12);
  add_note->cv_input = inputs->sr;

  wavetable_osc2->frequency_input  = add_note;

  mixer->input_1 = wavetable_osc1;
  mixer->input_2 = wavetable_osc2;

  this->last_module = mixer;

  this->last_module = multiply;
  */
}